%h1 Messages
- @message.each do |message|
  %h2= message.title
  %p= message.description
  = link_to "View message", message_path(message), class: "btn btn-default"
= link_to "Add new message", new_message_path, class: "btn btn-default"
